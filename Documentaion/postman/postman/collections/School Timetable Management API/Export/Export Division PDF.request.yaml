$kind: http-request
url: "{{baseUrl}}/export/division/pdf"
method: POST
headers:
  Content-Type: application/json
  X-Academic-Year-Id: "{{academicYearId}}"
body:
  type: text
  content: |-
    {
      "divisionId": "{{divisionId}}",
      "academicYearId": "{{academicYearId}}"
    }
order: 1000
