$kind: http-request
url: "{{academicYearBaseUrl}}/academic-years/{{academicYearId}}"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('Academic year retrieved', () => pm.response.to.have.status(200));
    language: text/javascript
order: 4000
