$kind: http-request
url: "{{academicYearBaseUrl}}/academic-years/{{academicYearId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.test('Academic year deleted', () => pm.response.to.have.status(204));
    language: text/javascript
order: 7000
