$kind: http-request
url: "{{academicYearBaseUrl}}/academic-years/health"
method: GET
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: pm.test('Health OK', () => pm.response.to.have.status(200));
    language: text/javascript
order: 1000
