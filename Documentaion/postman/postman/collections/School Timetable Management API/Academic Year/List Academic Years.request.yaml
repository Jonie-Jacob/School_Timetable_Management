$kind: http-request
url: "{{academicYearBaseUrl}}/academic-years?page=1&limit=10"
method: GET
queryParams:
  page: "1"
  limit: "10"
scripts:
  - type: afterResponse
    code: pm.test('List returned', () => pm.response.to.have.status(200));
    language: text/javascript
order: 3000
