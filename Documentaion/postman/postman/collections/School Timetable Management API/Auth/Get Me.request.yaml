$kind: http-request
url: "{{authBaseUrl}}/auth/me"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('Profile retrieved', () => pm.response.to.have.status(200));
    language: text/javascript
order: 4000
