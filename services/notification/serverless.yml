service: timetable-notification

plugins:
  - serverless-offline
  - ../../plugins/serverless-prod-config

provider:
  name: aws
  runtime: nodejs22.x
  stage: ${opt:stage, 'dev'}
  region: ap-south-1
  memorySize: 256
  environment:
    DATABASE_URL: ${env:DATABASE_URL, 'postgresql://timetable_admin:localdev123@localhost:5433/timetable_dev'}
    STAGE: ${self:provider.stage}
  httpApi:
    authorizers:
      cognitoAuthorizer:
        type: jwt
        identitySource: $request.header.Authorization
        issuerUrl: ${env:COGNITO_ISSUER_URL, 'https://cognito-idp.ap-south-1.amazonaws.com/local'}
        audience:
          - ${env:COGNITO_CLIENT_ID, 'local'}

custom:
  serverless-offline:
    httpPort: 4012
    lambdaPort: 4062
    noPrependStageInUrl: true

functions:
  notification:
    handler: src/handler.handler
    timeout: 30
    events:
      - httpApi:
          method: GET
          path: /notifications
          authorizer:
            name: cognitoAuthorizer
      - httpApi:
          method: GET
          path: /notifications/count
          authorizer:
            name: cognitoAuthorizer
      - httpApi:
          method: PUT
          path: /notifications/{id}/dismiss
          authorizer:
            name: cognitoAuthorizer
      - httpApi:
          method: PUT
          path: /notifications/dismiss-all
          authorizer:
            name: cognitoAuthorizer
      - httpApi:
          method: GET
          path: /notifications/health
